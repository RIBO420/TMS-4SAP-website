---
import BaseLayout from '../layouts/BaseLayout.astro';
import { withBase } from '../i18n/utils';
---

<BaseLayout
  title="Page not found | TMS 4SAP"
  description="The page you're looking for can't be found. Return home or explore our modules."
  lang="en"
>
  <section class="not-found">
    <div class="container">
      <p class="eyebrow">Error 404</p>
      <h1>We couldn't find that page.</h1>
      <p class="lead">
        The link may be broken, or the page may have moved. Let's get you back
        to something useful.
      </p>
      <div class="actions">
        <a class="btn btn-primary" href={withBase('/')}>Back to home</a>
        <a class="btn btn-secondary" href={withBase('/modules/')}>Explore modules</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .not-found {
    padding: 8rem 1.5rem;
    text-align: center;
  }
  .container {
    max-width: 720px;
    margin: 0 auto;
  }
  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-accent, #e63946);
    margin-bottom: 1rem;
  }
  h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.1;
    margin-bottom: 1.25rem;
  }
  .lead {
    font-size: 1.125rem;
    color: var(--color-text-muted, #64748b);
    margin-bottom: 2rem;
  }
  .actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    transition: transform 0.15s ease;
  }
  .btn:hover {
    transform: translateY(-1px);
  }
  .btn-primary {
    background: var(--color-accent, #e63946);
    color: #fff;
  }
  .btn-secondary {
    background: transparent;
    color: var(--color-text, #0f172a);
    border: 1px solid currentColor;
  }
</style>
